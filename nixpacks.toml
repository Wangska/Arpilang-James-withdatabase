[phases.setup]
nixPkgs = ['nodejs_18', 'npm']

[phases.install]
cmds = [
  'cd server && npm ci',
]

[phases.build]
cmds = [
  'cd split-easy-flex && npm ci',
  'cd split-easy-flex && npm run build',
]

[start]
cmd = 'cd server && npm start'

[variables]
NODE_ENV = 'production'
