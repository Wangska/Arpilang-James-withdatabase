[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = [
  'cd server && npm ci',
  'cd split-easy-flex && npm ci',
]

[phases.build]
cmds = [
  'cd split-easy-flex && npm run build',
]

[start]
cmd = 'cd server && npm start'

[variables]
NODE_ENV = 'production'
